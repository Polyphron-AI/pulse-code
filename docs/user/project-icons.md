# Project icons

Project icons remain available while reconnecting and after an offline restart. Changing an icon refreshes the cached image. Your selected emoji or custom icon takes priority over a discovered project favicon.

Removing an environment or clearing its client cache removes its stored icons.
