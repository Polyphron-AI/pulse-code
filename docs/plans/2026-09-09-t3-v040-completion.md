# Complete T3 v0.0.40 compatibility

The user authorized continuing toward full v0.0.40 compatibility, retaining three-agent implementation, commit/push, and preview deployment preferences. Start from `823079bb5`; target upstream `09e8de9c655ae85410bf6b00446f272a01da81c7`. Preserve Pulse's identity, OMP, Office/Talk, schedules, remote connection modes, and existing local edits. Do not claim completion by advancing a version label.

The previous inventory contains 1,054 source commits and 183 simulated conflict paths. Selected ports have landed. Full completion requires classifying the remaining changes against actual code, implementing applicable behavior, documenting intentional Pulse-specific differences, and validating each affected client and provider.

## Design and workflow

- [x] Gather current state, target, retained features, and prior verification.
- [x] Compare direct merge, isolated cherry-picks, and coordinated feature batches. Use feature batches while auditing the complete target diff, because direct replacement risks shipped migration IDs and Pulse-specific lifecycle behavior.
- [x] Reuse isolated integration worktree with previously verified baseline and installed dependencies.
- [x] Record the implementation and review boundaries before changes.
- [ ] Implement async questions as one coordinated server/client feature.
- [ ] Review migration, provider, mobile, settings, recovery, and release gaps against target source; create a complete disposition ledger.
- [ ] Implement and test the remaining applicable compatibility groups in dependency order.
- [ ] Independently review integration and run focused checks across affected surfaces.
- [ ] Commit/push verified integration, preserve local edits, and update the preview installer with matching native components.
- [ ] Advance baseline metadata only when the disposition ledger and verification support full compatibility.

## First coordinated batch: asynchronous questions

Backend owns contracts, generated Codex schemas, adapters, ingestion, durable lookup, decider/projector, and engine. Port `d76b24dd1`, relevant lifecycle lookup from `b17cc3d1b`, `d7fe47fd0`, `7ac93e300`, and the server/contracts portion of `7112697e8`. Message-mode requests use stable completed-item IDs, do not pause the turn, and answers atomically resolve the request and submit an ordinary user turn. Dismissal records resolution without submitting a turn. Preserve pending requests beyond activity retention limits.

Clients own web, mobile, and shared client runtime. Port async freeform parsing, shared finality from `e63ddb48e`, and client dismissal from `7112697e8`. Closed requests stay closed across clients. Do not reintroduce answered requests after reconnect or retention trimming.

OMP remains a blocking ACP callback with omitted response mode. Preserve cancellation and session isolation. Scheduled occurrences complete according to terminal turn state; later answers cannot rewrite a completed occurrence's handoff. Add focused regressions for both provider modes and scheduling boundaries. No new database migration is necessary for this batch.

The third agent audits migration and broad target compatibility while the coordinator checks shared contracts, reviews changes, integrates work, and maintains the completion ledger. Agents use separate worktrees and own disjoint files. No PR, live database writes, browser, audio capture, or desktop restart is needed for these source changes. Browser acceptance requires the existing permission rules; native iOS acceptance requires its platform.
