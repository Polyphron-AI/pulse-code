# T3 v0.0.40 completion ledger

Target: `09e8de9c655ae85410bf6b00446f272a01da81c7`. The [source disposition inventory](t3-v040-disposition.json) accounts for every commit in the original 1,054-commit interval. Its pending rows require semantic review. File overlap and successful patch application do not prove compatibility.

The target changes 2,389 paths from the declared baseline: 728 web, 507 server, 454 mobile, 155 client runtime, 132 desktop, 81 marketing, 73 shared, and 48 contracts. These counts describe source scope, not missing features. The audit inspected migration sources, all changed contract filenames, provider history, native dependency/configuration diffs, and selected implementations. It did not independently verify every path.

## Integrated checkpoint through `5a12b7e1e`

- The compatibility branch was pushed through `cefe4e166`. Preview source `4350ae4f3` preserves published `.20260910.3` Office, Talk, MCP/skill search, dictation, and downloads, and includes compatibility through `62d63d26a`. Its `.20260910.4` installer is being rebuilt with matching server/web/desktop metadata; publication is not yet claimed.
- Native media `8ee58fbb4` and lock `87c1849c6` passed 168 agent tests and mobile types. Android build 7 succeeded in 10m10s with 835 tasks. The isolated client paired, rendered seeded messages, expanded its composer, and opened Photos/Files and the document picker. The session ended before selection/removal was verified. Playback is not yet accepted.
- Chromium attachment paste and removal passed with no page errors. The harness needed a real ClipboardEvent; the earlier synthetic event did not reach the composer correctly.
- Reset-credit contracts, scoped commands, configured hub protocol corrections, pooled limits, and local `/usage-limits` clients are integrated through `f78e5d9d1`. Redemption remains explicit and confirmed. No real credits were consumed.
- Desktop continuation `62d63d26a` prepares eligible threads at installation, clears markers on failure, and preserves them only after accepted handoff. Its 24 focused tests and server types passed. Nightly release-note counts/links through `42924220b` preserve Pulse rollback feeds; 56 backend/desktop and 46 web checks passed.
- Import foundation `8e34a5a9c` passed 328 focused agent tests. Copy-reference `fed143810` and stop shortcut `33065a21a` passed focused tests and web/mobile types; the new Android keyboard module needs a rebuild. Setup prerequisite `5a12b7e1e` passed 141 agent and 23 integrated client-runtime checks. Onboarding UI remains in progress.
- The preview merge passed server, contracts, web, desktop, and mobile types. Merged evidence includes 58 backend assets, 36 desktop/shared media, 79 web, 30 follow-up client, and 67 packaging/Office/Talk checks. These overlap agent suites and must not be summed as unique tests.

## Earlier checkpoint through `8eb5ab63d`

- Normalized subscription limits and explicitly configured quota sources now span contracts, backend, web, and mobile. Reset-credit actions and subsequent quota UI refinements remain a separate coordinated batch.
- Web sidebar arrangement now supports cross-section destinations, action labels, empty targets, pointer cancellation, and cleanup while preserving Pulse row actions. Agent validation passed 235 focused tests and web types.
- Shared media parsing, signed host media streaming, and web/desktop image, video, PDF, and HTML viewers are integrated. Mobile media, tool-result viewed images, and automatic workspace refresh remain incomplete. These source groups are recorded as partial.
- Integrated verification passed 82 focused settings, limits, pointer, and media tests, two config-stream tests, and web types. The merged server test imported Ref twice; the duplicate was removed before the backend typecheck retry.
- Isolated Chromium loads the seeded thread with an available provider and no page errors. Three settings passed save, reload, and reversal checks. The fixture now stores the canonical `codex` driver key.
- Android debug compilation succeeded with 835 tasks using a short disposable checkout and bounded build workers. The isolated emulator pairs and displays the nine seeded threads across three projects. Thread interaction and attachment acceptance are still pending; a list-rendering pass is not full mobile acceptance. Upcoming Expo Video support requires a rebuilt native client.

The earlier progress entries below retain their original evidence dates; this checkpoint supersedes their pending labels only for the behavior explicitly listed here. No installer containing this checkpoint has been published yet.

## Dependency order and evidence

| Group                                  | Source                                                                                                           | Status and integration direction                                                                                                                                                                                                              |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Correctness and initial protocol fixes | Prior [correctness](upstream-sync-2026-09-09.md) and [protocol](t3-protocol-compatibility-2026-09-09.md) ledgers | 17 selected changes landed and verified.                                                                                                                                                                                                      |
| Async questions                        | `d76b24dd1`, narrow lookup from `b17cc3d1b`, `d7fe47fd0`, `7ac93e300`, `e63ddb48e`, `7112697e8`                  | Coordinated backend/client implementation underway. Preserve OMP blocking callbacks, schedules, request finality, and pending retention.                                                                                                      |
| App/MCP approvals                      | `7c6163c67`                                                                                                      | Confirmed protocol gap: request kinds, app names, approval options, and answer mapping need adapters, contracts, and web/mobile UI together.                                                                                                  |
| Pairing metadata                       | `9d28c21a2`                                                                                                      | Integrated as `2886564a7`. Credentials only come from creation responses. Older clients with required credential fields need coordinated updates.                                                                                             |
| Used-provider restoration              | `06de9e90a`                                                                                                      | Implementation underway. Restore previously used providers after upgrades while preserving explicit disabled state and OMP.                                                                                                                   |
| Bounded reconnect replay               | `7e460f429`                                                                                                      | Integrated as `d272b7661`. SQL row and 8 MiB serialized-byte preflight runs before decoding replay. The earlier projector-backlog fix does not cover this.                                                                                    |
| Restart continuation                   | `0929907ff`, `5b7d72aad`, `b906ce2d7`, `1abc717f0`, `f47a3fe90`                                                  | Persisted opt-in, startup reconciliation, interrupted-session recovery, and shared-setting propagation. Preserve explicit provider capabilities and scheduled occurrence history.                                                             |
| Provider permissions and stop          | `43f723f80`, `994bd7373`, `d2b6f3b92`, `01f3e50ec`, `6134b90ff`                                                  | Review and port Cursor/OpenCode permissions, stop, and transport-error behavior.                                                                                                                                                              |
| Discovery and CLI fidelity             | `ea71a19d4`, `18573d60a`, `80a14b658`, `bc918e74a`, `15fea6c5f`, `2152d44de`, `a434677ec`                        | Coordinate Claude/project skills, large OpenCode skill results, and Grok CLI protocol. Preserve OMP registration.                                                                                                                             |
| Claude SDK/results                     | `560afffde`, `a5bbad910`, `940e8233c`, `4c7cd17a8`, `3d00cfd5a`, `3bf74eb6d`                                     | Upgrade SDK ^0.3.170 to target ^0.3.260 together with result/error/usage handling and bundled binary overrides.                                                                                                                               |
| Compaction/models/usage                | `c7222ca4d`, `c5ba51d62`, `5fa35d211`, `035428368`, `5a433244d`, `1587f248d`, `19d8ab2ae` and limit follow-ups   | Coordinate adapter capabilities, contracts, and clients. Preserve Pulse usage behavior; do not port reverted `535557b3f` alone.                                                                                                               |
| Project/thread persistence             | Migration groups below                                                                                           | Changes need decider/projector, snapshots, and clients, with schedule regressions.                                                                                                                                                            |
| Scoped defaults and balancing          | `9f40b2f56`, `420fd76f6`                                                                                         | Larger coordinated groups with explicit environment ownership and retained schedules/Office.                                                                                                                                                  |
| Mobile native stack                    | `3e6ab36f6`, `aab404964`, `b5a09e13f`                                                                            | Expo 57.0.18, RN 0.86.3, Reanimated 4.5.1, Worklets 0.10.1, Metro 57/screens 4.26.2 patches, exact sharing 57.0.17. Upgrade native binaries and lockfile together. Preserve Pulse fingerprints and schemes. iOS acceptance needs macOS/Xcode. |
| Remote diagnostics                     | `94f194816`                                                                                                      | Add DPoP reason fields across server/relay/clients; preserve Pulse client identities.                                                                                                                                                         |
| Media/import/setup/browser             | `8f4913221`, `beae2147a`, `775129984`, `09aac7156`, browser-profile/import series                                | Feature parity requires separate review. These are not prerequisites for decoding Codex protocol messages.                                                                                                                                    |

## Integration evidence in this batch

As of `99dcf656a`, integration includes the following additional groups. The original direction table above records the initial audit; the evidence below supersedes its in-progress labels. Full V40 compatibility is not yet established.

Later integration through `11ea38d5e` adds the following evidence:

- Generic attachments now include web file picking, bounded HEIC conversion, and mobile picker/share/draft/outbox flows. The HEIC dependency is resolved in the lockfile. Integrated web and mobile typechecks pass. The SDK 57 Android development APK compiled successfully (835 tasks) using shorter paths in a disposable checkout; emulator acceptance is underway.
- Manual compaction backend and both clients are integrated, including streamed Claude context accounting, declared adapter strategies, command availability, the web context-meter action, mobile status and terminal result rows. OMP remains explicitly unsupported for compaction. Agent checks passed 316 unique backend and 178 client tests. Integrated server typecheck passed before the later model/default batches.
- Machine/project defaults (`0c2885cb8`), bundled V40 model metadata (`7c71c12a7`), custom model descriptors (`598617718`) and opt-in environment balancing (`482b0acb9`) are integrated. Metadata stays fixed to the reviewed target rather than fetching mutable upstream-main configuration. Existing model slugs, Pulse model-list controls, OMP and user-selected draft context are preserved.
- Mobile active-thread arrangement (`f47d365c5`) passed 69 focused tests and mobile typecheck. Mixed-version environments cannot use hidden legacy auto-settled rows as arrangement anchors. Web drag-and-drop follow-ups remain in progress.
- Chromium verification exposed a configuration startup race. `7e886c0f3` acquires the settings subscription before loading the snapshot. Three focused WebSocket regressions pass. The isolated browser passes persistence and reversal checks for restart continuation, merge settlement and inactivity settlement with no page errors. Thread messages render; provider selection and attachment interaction acceptance remain under investigation.
- `11ea38d5e` corrects the composer label so an unavailable provider or required project selection is not described as a disconnected environment. Its 20 focused component checks pass.
- The first Android JavaScript load reached the app, then Metro exhausted Windows file handles while loading a dynamic filesystem module. This is not a passing mobile acceptance run. Metro is being retried with limited workers and an IPv4 endpoint reachable from the task-owned emulator. All test state is disposable and local.

- Generic approval consent was corrected in `2beed4c30`: plain Approve cannot silently select persistent approval. Restart continuation and shared settings are integrated; desktop update-specific continuation remains unadvertised until its handoff is implemented.
- Skill discovery and invocation now cover the adapted providers and web/mobile composers (`4ded10fe5`, `5bb361908`). Grok ACP support is integrated in `caa141a1d`, with 133 focused tests and one Windows-inapplicable POSIX signal case skipped. These checks use synthetic provider protocols.
- Settlement follow-ups, project auto-pull, project icons, shared PR summaries and branch associations are integrated through migrations 45–50. Pulse migration 46 intentionally preserves historical model choices because automatic and explicit selections cannot be distinguished safely. Migration 48 repairs only automatic settlement projections and preserves manual and scheduled history.
- Remote desktop updates are integrated in `7b3a10daf`, including prepare/commit coordination, reconnect verification, rollback selection, cancellation, and recovery of backend processes after failed installation. Focused updater, registry, lifecycle and client tests passed. A real installer update has not yet been exercised; newer-nightly changelog handling remains separate.
- Active thread ordering persistence is integrated in `76a39458f` with Pulse migration 51. The agent's 186 focused checks passed. Reordering controls and later drag-and-drop behavior are separate work; this foundation does not establish their completion.
- Signed attachment contracts, backend upload/download, web upload retry foundation, and shared helpers are integrated through `99dcf656a`. File uploads are bounded at 50 MiB and images retain their existing 10 MiB limit. Web generic file selection and mobile share/draft flows are still being integrated. Backend evidence includes 60 contract, 71 core/projection/capability, 54 provider/OMP and three real signed-route checks; these are agent-scoped results before final integrated client acceptance.
- The authorized isolated Chromium check loads synthetic projects and General settings without page errors. Settings persistence/reversal checks and Android emulator acceptance remain in progress. Android prebuild succeeds, but Windows native path limits and host memory pressure have delayed producing a test APK; no emulator pass is claimed.
- The published preview advanced independently to `pulse-preview-20260910.1` while this branch was being integrated. Packaging must start from the latest preview lineage to preserve its Office, Talk/dictation, MCP defaults and skill-search work. These later compatibility commits have not yet been published in an installer.

- Async questions are integrated across the provider, durable command handling, shared client runtime, web/desktop, and mobile. `d0ad3d1bf` adds terminal-first SQL lookup and retains the terminal activity for any request still visible in bounded snapshots. This prevents resolved questions from reappearing after reconnect. Backend verification included OMP callbacks and schedules; client verification included answer, dismissal, and native option mapping.
- App/MCP approvals are integrated in `4e5b75aea`, `27998f9ee`, and `4952af576`, including file-change details from `57a6b70e2`. Backend coverage passed 106 focused tests and client coverage passed 115. Independent review found that a persistence-schema default could broaden generic approval; its correction must land before release.
- Used-provider restoration is integrated in `82a32545f`, including explicit disabled-state preservation and OMP's existing default. Verification passed 133 focused tests; four existing Windows path-fixture cases were excluded after baseline comparison.
- Pairing metadata passed 23 focused store, admin, CLI, client, HTTP, and WebSocket checks. Bounded replay passed 17 focused SQL/subscription checks, including UTF-8 byte accounting and snapshot fallback. Matching clients must ship with the server because older pairing-management schemas require the removed credential field.
- Mobile SDK 57 installation and TypeScript checks pass. Android development prebuild passes with Pulse identity and schemes preserved. Expo is pinned to 57.0.18 across peer dependencies; its bundled dependency matrix accepts the patched sharing 57.0.17 and constants 57.0.16. Expo's online checker recommends newer releases, so that check is not evidence for the fixed V40 target. Native compilation and device acceptance remain pending.

## Migration allocation

Pulse has shipped `041_Issues` and `042_Integrations`. Preserve them. Allocate the upstream migrations in the same relative order using the following free IDs, only when their dependent behavior is ready.

| Upstream ID | Pulse ID | Migration                           | Source      |
| ----------- | -------- | ----------------------------------- | ----------- |
| 41          | 43       | AuthSessionClientConnection         | `11f051373` |
| 42          | 44       | ProjectionThreadLinkedPullRequest   | `3c75eb113` |
| 43          | 45       | ProjectionThreadsUnsettledAt        | `3b86ef941` |
| 44          | 46       | ClearAutomaticProjectModelDefaults  | `5392c9bb9` |
| 45          | 47       | ProjectionProjectsAutoPull          | `ba3cb0773` |
| 46          | 48       | RepairAutomaticSettlementTimestamps | `2971ec320` |
| 47          | 49       | ProjectionProjectIcon               | `f6c04c552` |
| 48          | 50       | ProjectionThreadBranchPullRequest   | `223ff4490` |
| 49          | 51       | ProjectionThreadsActiveOrderKey     | `2d645df47` |

The project-model repair rewrites historical creation events based on an upstream UI assumption. Check whether Pulse let users choose a model at project creation before applying that rewrite. Settlement repair must preserve scheduled-thread history. No migration is approved merely by allocating an ID here.

## Pulse ownership

Retain branding, CLI aliases, public endpoints, release channels, schemes, and relay identities accepting Pulse and legacy T3 clients. Preserve OMP, schedules, Office/Talk, mail dependencies, and applicable Office fixes promoted to main after preview .3.

Upstream marketing, analytics destinations, hosted infrastructure, and stable-release automation are not requirements for application compatibility. Upstream thread-feedback upload `3db38b881` is a separate data-flow decision; do not enable automatic content transmission as a side effect of protocol updates. Browser-cookie import and paid usage redemption remain explicit user actions even if their implementation is imported.

The upstream baseline remains unchanged until pending source dispositions and cross-surface verification support advancing it.
