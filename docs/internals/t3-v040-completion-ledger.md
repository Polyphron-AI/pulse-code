# T3 v0.0.40 completion ledger

Target: `09e8de9c655ae85410bf6b00446f272a01da81c7`. The [source disposition inventory](t3-v040-disposition.json) accounts for every commit in the original 1,054-commit interval. Its pending rows require semantic review. File overlap and successful patch application do not prove compatibility.

The target changes 2,389 paths from the declared baseline: 728 web, 507 server, 454 mobile, 155 client runtime, 132 desktop, 81 marketing, 73 shared, and 48 contracts. These counts describe source scope, not missing features. The audit inspected migration sources, all changed contract filenames, provider history, native dependency/configuration diffs, and selected implementations. It did not independently verify every path.

## Published V40 and Warden preview `.8`

[Pulse preview 20260910.8](https://github.com/Polyphron-AI/pulse-code/releases/tag/pulse-preview-20260910.8) is published as a prerelease from `0662eb65014be8a08840f46dc5005754cf9a7767`. The integration branch through `ce0ddb3758e46ab6d4b958297344954e16cc514d`, preview branch and exact source tag were pushed together. GitHub's latest stable release remains `v0.0.37`.

This adds Warden contracts `49e841c33` and runtime `7260c2897`, with V40 aborted-turn cleanup and Antigravity handoff adaptations. The preview merge preserves external MCP configuration and managed skills. Warden is off by default and requires catalog version 3 plus a separately configured compatible broker and enrollment; older catalogs are rejected. See [integration and activation limits](v40-warden-integration.md).

The final preview passed 307 focused Warden/settings/MCP/provider/skill tests, including an explicitly enabled cross-repository synthetic Go broker and CLI parity test. Server, web, mobile and desktop typechecks passed. Chromium verified Warden default-off, enable/reload and reset/reload after waiting for environment synchronization, plus saved-output download bytes, Office/Code switching and the single dictation control, with no page errors. The initial browser attempt clicked before synchronization and was corrected in the test helper; it did not require a production change.

The unsigned Windows x64 installer is **179,542,624 bytes**, SHA-256 `a0e5c34367dd7027e77e707fb7aec81cbb9e046cf51a701137b4454575b75b98`. Exact source/version, Warden payload, Electron 43.4.1, Office/Talk dependencies, Talk/resource-monitor protocols and the matching WSL prebuild passed package verification. All seven uploaded GitHub asset sizes and SHA-256 digests match their local files. The release is published, not a draft, and is not marked latest stable.

The fixed V40 target and source inventory remain unchanged. Full V40 compatibility, installed-app updates, full WSL server acceptance, live Warden/provider/Grafana flows, Warden remote/tunnel behavior, Windows credential-directory ACL review, native media playback and iOS acceptance remain incomplete. Source Warden PR CI is not green on existing scheduler formatting and two unchanged client-runtime timeouts; scoped checks do not establish full-suite acceptance.

## Final client-check checkpoint at `c5c2c439d`

Integration is at `c5c2c439d3c0844db99fbffddcd5d3a47df93714`. The fixed target, declared baseline and source inventory remain unchanged; the inventory still assesses `af666bb52`. Implementation disposition and acceptance remain separate, and full V40 compatibility is not claimed.

The primary agent completed isolated browser host-document read-only and relative-link checks, Office/download preservation and the single voice-control check with `PAGE_ERRORS=[]`. The prepared preview passed 124 focused seam tests and four surface typechecks. Android's synthetic 47-byte background attachment retained one server asset across cold restart. Offline **Queue task / Sends on reconnect** state also survived cold restart; the named pending item was deleted through the UI and its absence confirmed before restarting the backend. No provider turn was sent during these checks.

The earlier Git status stream diagnostic is not a compatibility blocker. Read-only investigation found harmless cancellation noise: cancelling an Effect beta.103 subscription produces `Cause.Done`; a late exit follows the interrupt after the RPC client has removed the request entry. The client ignores that absent-entry exit before decoding it. The fixed target uses the same stream path, both browser preservation markers passed, and no production change was required.

Preview `.7` source is `b17319db69a5733f35c5aff3bfca8a71e18aa669`. It was built and then repackaged with the WSL prebuild, containing 83 native-package files and 36 native binaries. Final package verification passed: **179,571,141 bytes**, SHA-256 `c689b569bce5e436b87a31ad9e2fbe5a28d97e9e7c9be264163d2cf4c70aa573`, Authenticode status **NotSigned**. The artifact directory is `.worktrees/upstream-claude-20260909/.t3/preview-release/v40-20260910-7-wsl`. Electron 43.4.1, the WSL binary hash marker, Office/Talk dependencies, server version, Talk hello/status/list/shutdown and resource protocol 3 checks all passed. It remains **unpublished**, with publication approval pending. The older `.6` artifact remains unchanged.

The Linux x64 `node-pty` 1.1.0 prebuild is 79,968 bytes, SHA-256 `0934354174ea1bb78a6e1f89b4e9d48e4167bd470157508ef0ee0bd0fa4ad7f5`. It was built from the exact package sources using the CI recipe; real synthetic PTY marker/exit checks passed under Linux Node 20 and 22. It requires glibc 2.34 or newer. This establishes the binary's scoped PTY behavior, not full packaged WSL server acceptance.

Installed desktop updates/rollback, full WSL backend startup, real provider login/turns, native recording/audio/media/sidecars and every remote/relay combination remain acceptance limits. No publication or blanket native acceptance is implied by the completed isolated checks.

## Current integration through `af666bb52`

The fixed target and declared baseline are unchanged. The refreshed inventory assesses `af666bb523d9309e0334babf5fd3206482ff700b`: **216 ported-reviewed, 10 partially-ported, six target-files-identical and 822 needs-disposition** rows, totaling the original 1,054 sources. These counts describe disposition, not a compatibility percentage. Earlier sections retain historical checkpoint evidence; their pending lists are superseded by the closures below.

- **Mobile background attachments:** `26ddd86ad` completes the functional upload portion of `9bc7a5684` alongside the existing account archive, adapts reference-aware cleanup from `ce4712d5b`, and supports queueing during upload from `66a24d6c1`. All 181 focused tests across nine suites, mobile types and scoped lint passed. Pulse inline writers, version 4 outbox, account/environment ownership and persist-first sends remain intact. See [upload behavior and proof](v40-mobile-background-uploads.md).
- **Internal activity reads:** `8018d7dd0` completes the bounded `b17cc3d1b` slice with 132 focused tests and server types. Pulse context-compaction inference keeps its necessary activity kinds; public snapshots retain their full behavior. See [narrow activity reads](v40-narrow-activity-reads.md).
- **Automatic title retries:** `6bd0e9c67` implements `fc262f1a2` with two exponential retries and four focused tests, including TestClock and a persisted title-update receipt. `da50e197f` separately corrects an existing compaction test to await its failure event before draining; its assertion, server types and lint pass. No production compaction change was needed.
- **Host documents and relative links:** `8f38c69ef` implements `f46a709ee` for host Markdown/HTML/PDF, with 152 focused tests and web/mobile/server types. `af666bb52` corrects Markdown sibling resolution relative to the document directory; 81 focused tests passed. Signed exact-file assets, read-only host Markdown and workspace-relative write boundaries remain intact. See [host document previews](v40-host-document-previews.md).
- **Electron runtime dependency:** `347b9d5d4` implements `4a9d2d0ce` at Electron 43.4.1. Desktop types, 128 focused tests and an isolated no-window custom-protocol/media-handler smoke check passed. Native capture, installer updates and packaged acceptance are separate. See [Electron 43 evidence](t3-v040-electron43-upgrade-2026-09-10.md).
- **Startup navigation:** `3b53cc2e5` fixes a confirmed Pulse race: delayed automatic index draft defaults no longer overwrite a subsequently selected route. Four delayed-defaults tests cover fresh/reused drafts and preserve explicit New Thread actions; web types and lint passed. This is an integration correctness fix, not a new upstream source row.

### Prepared `.7` client evidence and release boundary

Preview merge `5d8e963c5` includes compatibility through `af666bb52` while preserving Office/Talk and download work. The primary agent reports isolated browser host Markdown opening read-only and relative sibling navigation passing, with `PAGE_ERRORS=[]` in `preview7-host-documents-fixed`. Android's current native attachment flow delivered a synthetic **47-byte** pending file to the server. The uploaded reference persisted across a cold restart without a duplicate upload. Offline queue and removal acceptance are still underway; no provider message was sent by those checks. Browser Office/download checks also passed with no page errors; 124 preview seam tests and four surface typechecks passed. A Git stream error is being investigated and remains unconfirmed; it is not established as a new compatibility blocker.

The `.6` artifact remains unchanged and unpublished: preview source `0237af5b1`, **173,635,834 bytes**, SHA-256 `c287a5a8a0162b1052bdbc06441c1e34422ae8f1f68f675f4d9ccf74674fb619`. Root verified packaged Office/Talk and resource checks. Its compatibility input is `5c96f4218` and its Electron runtime is 41.5.0, so the later changes above are outside that artifact. The `.7` version edit is uncommitted in the preview worktree. **No `.7` build or publication is claimed.**

Real provider login/turns, installed desktop updates/rollback, native capture/audio/media/sidecars, platform-specific rendering and all remote/relay combinations remain acceptance limits. Functional upload implementation is now covered; current Android receipt evidence alone does not prove its full native lifecycle. Automatic feedback/analytics transmission remains a separate data-flow decision. No full V40 compatibility claim is made, and overlapping test totals must not be summed as unique checks.

## Frozen `.6` compatibility checkpoint through `5c96f4218`

The compatibility input for the next `.6` preview is frozen at `5c96f42183178b1fc2bb3be3efa273fb92ade8c4`. This is a source checkpoint, not a built or published release. The fixed V40 target and declared upstream baseline are unchanged. The inventory contains 210 ported-reviewed, 12 partially-ported, six target-files-identical and 826 needs-disposition rows across the original 1,054 source commits. These counts are dispositions, not a compatibility percentage.

- **Legacy project scripts:** `09b23c434` implements `d8bc6831c`. Existing script IDs remain runnable, editable and removable even when they cannot support a shortcut; new settings IDs validate before persistence. All 38 focused backend/web tests, server/web types and scoped lint passed. Native scripts already use saved IDs directly. See [the bounded backend review](t3-v040-backend-final-review-2026-09-10.md).
- **Claude subagent model ordering:** `5c96f4218` implements `6a2608292`. An assistant snapshot that precedes task registration retains its authoritative model in a 64-entry pending map. Six focused adapter tests cover event ordering, effort and oldest-entry eviction; server types and scoped lint passed.
- **Antigravity diagnostics:** `a6b99dcd3` implements `6349a0e68`. A post-authentication session/model initialization failure receives a distinct safe message. Fourteen focused tests passed. This does not establish successful real-provider installation, login or turns.
- **Mobile account-owned unsent work:** `1e557395f` adapts the archive slice of `9bc7a5684`. Drafts and queued messages are durably backed up before removing relay environments and restored only for their owning account. Backup failure prevents destructive cleanup; late delivery and concurrent edits preserve ownership. All 74 focused tests, mobile types and scoped lint passed. Background attachment uploads are not part of this slice. See [mobile relay draft preservation](v40-mobile-cloud-draft-archive.md).
- **Prepared-preview verification:** `618cc17fa` contains compatibility through `9480709d4` and preserves the existing Office, Talk and download work. Root reports passing isolated browser checks for download behavior, Office and a single voice control, plus 46 focused seam tests and web types. These checks apply to that prepared preview, which predates the frozen script, diagnostic, archive and Claude changes above. They are not installation, packaging or publication evidence for `.6`.

Remaining work is explicit: the background attachment-upload queue from `9bc7a5684` and queue-while-uploading follow-up `66a24d6c1`; host document previews outside a workspace from `775129984`; broader internal activity-read optimization from `b17cc3d1b`; and automatic title retries from `fc262f1a2`. The latter performance, retry and external-document batches are separate from this frozen input. Pulse retains its inline image writers, file-backed readers and current outbox ownership until a coordinated upload change is verified.

The `.6` preview merge, artifact build, installer acceptance and publication are not claimed here. Earlier isolated Chromium/Android checks cover their recorded paths only; real provider login/turns, installed desktop updates, native media playback and every remote/relay combination remain unverified. No full V40 compatibility claim is made. Test counts overlap earlier and integrated suites and must not be added as unique totals.

## Earlier checkpoint through `cd25876d3`

This checkpoint includes `b50173ab8` plus the PR facet follow-up `d2e6a14d7`, integrated as `cd25876d3`. The fixed target and declared upstream baseline are unchanged. The inventory now has 207 ported-reviewed, 11 partially-ported, six target-files-identical and 830 needs-disposition rows across the original 1,054 sources. These are source dispositions, not a compatibility percentage.

- **HTTP credentials and active sessions:** `d872cd739` implements source `363cde411`. HTTP requests resolve the current token and endpoint, share one renewal per environment/account/key, and retry one rejected credential without replacing a healthy socket. Account changes, signing-key rotation, cancellation, late rejection and persistent-write races are guarded. Connected expired sessions remain listed until disconnect; credential expiry still applies to new requests. Web/mobile reopen tests preserve unrelated saved secrets. Agent verification passed 172 tests across 14 focused files and client-runtime, web, mobile and server types. Pulse's existing connection composition and generic PR authentication error remain in place.
- **Terminal bounds:** `1f8cc75c5` implements `3bbbc1d9f` and `cf9729d5e`: retained history has byte/chunk bounds alongside Pulse line limits, handles Unicode boundaries, coalesces persistence, and restores a bounded suffix of current or legacy history. All 79 focused Manager tests and server types passed. See [terminal runtime](terminal-runtime.md).
- **Desktop and shell wire compatibility:** `5932b788e` implements `2fbe31309` with a desktop runtime tab-ID codec while retaining the public ID limit; eight IPC tests and desktop types passed. `b50173ab8` addresses Pulse-origin schedule events: clients opt in, while other clients receive compatible snapshots that advance the cursor through schedule-only replay. Live, replay, resync, ordering and byte-budget cases passed 27 focused tests, plus server/client-runtime types. This is a Pulse adaptation, not a newly ported upstream source. See [wire residual fixes](t3-v040-wire-residuals-2026-09-10.md).
- **Mobile work and branch context:** `1b7c5115a` implements `d6aa179ad` outbox indicators with 90 focused list/home/order tests. `a0649184b` implements `c4353bc6b` file-backed image readers with 144 focused tests; existing inline writers and outbox ownership remain. `ad938a51c` implements `7dda0b1c0` existing-branch task creation with 20 focused tests and mobile types. These are synthetic/static checks, not acceptance of every native flow. See [mobile unsent work](v40-mobile-unsent-work.md).
- **Persistent project icons:** `d1fcb1105` implements `eee05575e`. Favicon metadata and cached bytes survive reload/reconnect; signed URL rotation, revision changes, removal, environment clearing, delayed writes and native disposal are covered. The initial 36 tests across five suites passed, followed by an overlapping 20-test cache retest; do not add those counts. Web, mobile and client-runtime types passed. See [favicon persistence](v40-project-favicon-persistence.md).
- **PR facets:** `cd25876d3` completes the functional `d35c71d1b` follow-up after the author/label controls. Suggestions request the existing PR-list API only while open and preserve environment, project, host and involvement scope. Counts describe the loaded sample, with author merge-history ordering, case-insensitive labels and row label metadata. All 111 focused tests across three files and web types passed. Pulse retains Apply/Clear controls, its sticky-title/menu presentation and independent selected-PR context; no additional backend API was needed.
- **Prepared preview:** `3f5ef84d5` merges the prepared `bf245de51` preview with compatibility through `ad938a51c`. The merge was clean, retained Office/Talk/download work, passed 176 focused tests across 19 files, and passed web, mobile, desktop, server, contracts and client-runtime types. This prepared preview does not include the later favicon, schedule or facet batches above. It has not been versioned, built or published. The prior `.5` local artifact remains unchanged; public push/publication remains pending after automatic approval review rejected the public action. Root owns subsequent isolated runtime verification and packaging; no new client acceptance is claimed here.

The project-script legacy failure (`d8bc6831c`) and broader internal activity loading from `b17cc3d1b` remain under review at this checkpoint. Full V40 compatibility is not claimed. Test totals above overlap earlier and integrated suites and must not be summed as unique checks.

## Earlier checkpoint through `cdb236d96`

- Mobile draft rows, multiple drafts, queued-message editing and recoverable thread creation are integrated through `65717a26b`, followed by direct/relay coexistence, model-source labels and variant Keychain configuration through `5c84e411c`. Agent checks passed 201 unsent-work and 38 connection/model/config cases. Integrated mobile types and 29 mobile/ingestion seam tests pass. The Android cold-start check exposed missing list-mount hydration; `5365063ed` fixes it with 49 focused checks. Two synthetic drafts now appear after cold start without opening a composer. Discarding one retains the other.
- Attached skills, task, approval, question, plan and stash drawers are integrated through `685c0c755`. Source 792a behavior is reviewed; Pulse retains banner and sync-cap geometry where severity, actions and dismissal are equivalent. The PR actions/list source 443 is reviewed with Pulse layout adaptations. `57267a104` adds author/label filters; d35 facet suggestions/counts and secondary refinements remain separate.
- `8571af150` attaches scroll listeners after a delayed timeline mount. `ecd14ff28` defers restoration until LegendList publishes its frame-coalesced scroll state. Chromium now passes scroll collapse, interaction restoration and multiline protection with no page errors. Panel open/close and reduced-motion activation/restoration also pass; settings and citation acceptance from the earlier checkpoint remain recorded separately.
- `548f5f3d9` uses probed scoped model names in Claude pause notices; `9ad628cbf` retains runtime item IDs for tool updates and coalescing. `0f580cbc7` isolates remote web cookies by persisted environment identity with validated legacy migration. Forty-one auth/identity tests and three HTTP migration/precedence tests pass, and integrated server types pass through cookie isolation.
- `e258387b7` defers attachment cleanup until the outer command transaction commits; its agent checks passed 59 engine/projection/activation tests. `cdb236d96` adds bounded relay restart backoff with stable-uptime/configuration reset; 11 agent tests pass. Terminal byte budgets and expiring remote HTTP credential refresh remain active follow-up work.
- Next preview source is prepared as `bf245de51`, merging original preview `2d86d084e` with compatibility through `685c0c755`. Office/Talk/download contexts and one dictation control are preserved. Frozen install, all four scoped typechecks and 166 focused merge checks pass. It has not been versioned, built or published, and does not include later commits. The prior `.5` artifact remains unchanged. Public push/publication approval is still pending after automatic review rejection. Full V40 compatibility is not claimed.

## Earlier checkpoint through `1f9a93d7b`

- Browser link preferences, native/browser tool icons, contrast, panel motion, assistant citations, proactive panels and composer scroll preferences are integrated. Citation authoring, save/reload/removal/cancellation passed in isolated Chromium. Contrast and panel-duration settings passed change/reload/reverse checks with no page errors. Actual panel motion and scroll-collapse acceptance remain open; preference persistence alone does not establish them.
- `68c34d9f1` captures checkpoints after terminal events and separates PR status refresh from capture. `1b909d54d` renders image tool results and refreshes workspace views after mutations while protecting pending edits. The merged checkpoint/projector/client-reducer/media-refresh suites passed 78 tests. Media presentation and stream helper suites passed another 15 distinct tests.
- `03b794acd` bounds live-event buffers, coalesces consecutive tool updates and measures projected client payloads. Agent validation passed 32 focused helper, subscription, replay and startup tests. `1ca20ac2c` through `cd04470cd` preserve drafts on storage failure, retain transport retries, carry drafts across environments, guard IME input and batch shell/sidebar updates. Agent validation passed 119 focused cases and scoped client types.
- `1f9a93d7b` persists PR filters and sidebar destinations, keeps selected detail open during filter changes, and adds readiness/updated/age/size sorting while retaining search relevance. Its agent checks passed 103 cases and web types. Remaining mobile unsent-work behavior and composer drawer portions are being ported separately.
- Integrated web types passed through citations and panel motion; server types passed through proactive panels. Later merged batches require final scoped typechecks. The isolated Android client loaded the seeded thread and rendered its messages; earlier attachment add/remove acceptance remains valid, but playback and installed desktop update acceptance remain unverified.
- Preview `.5` remains built from its earlier `2d86d084e` source, unpublished and unchanged. None of this checkpoint is included in that installer. Publication is awaiting the explicit public repository/payload approval requested after automatic review rejected the push. Full V40 compatibility is not claimed.

## Earlier checkpoint through `189b422b2`

- `bc7d484af` adds explicit rate refresh with a one-minute fetch floor, retaining custom prices and selected-environment rescans. Eleven synthetic usage tests pass. `3bead9b18` adds terminated thread-load reporting while preserving Pulse's current lifecycle and retry behavior; 42 focused tests pass. `f05a2ed50` adds environment theme publishing and opt-in adoption. Five integrated server stream tests pass, including old-client gating and settings snapshot race coverage. `7b91bfa7f` accepts both reset-credit error formats; four contract compatibility tests pass.
- `fa882ba40` and `30dd5cb09` restore skill-menu settings/source badges and the opt-in context indicator. Pulse compaction and usage widgets remain. The skill preference passed a disposable Chromium toggle/reload/reverse check with no page errors. `189b422b2` adds environment-scoped file reveal and Windows drive/reference-link fixes; agent validation passed 143 tests with nine POSIX-only fixtures skipped on Windows. Actual file-manager launch was not tested.
- Integrated server types pass through environment themes/rate refresh; mobile types pass through the skill-menu checkpoint; web types pass through the preceding correctness/machine batch. Later client batches still require final integration checks. The built `.5` artifact remains unchanged and unpublished, and does not include these follow-on commits.

## Earlier checkpoint through `0bb876d37`

- Follow-on commits `8eb44cb59`, `04d3395d8`, and `5b3f672b6` add optional image dimensions in signed asset responses and environment machine/WSL metadata. Header/asset regressions passed 34 tests; integrated machine/icon regressions passed 17 tests. `954ee7e87` adds normalized complete, partial, and unavailable turn token usage while preserving legacy raw usage and omitting analytics-only transmission. Its agent checks passed 333 cases; two pre-existing Codex child-agent lifecycle mismatches are being addressed separately.
- The integrated Chromium composer/attachment regression passed after restarting the disposable backend at `0bb876d37`, with no page errors. This is not real Antigravity provider acceptance.
- Antigravity contracts, ACP transport, backend setup, web/desktop settings, account model selection, mobile send gates, and web composer capability handling are integrated through `8e6f499ce`. Existing Pulse provider IDs, compaction, usage panels, dictation, and browser profiles remain. Agent checks passed 573 backend/contract/ACP cases, with five platform skips, and focused client suites. Integrated web, server, and mobile typechecks pass. No real Antigravity installation, account login, or provider turn was performed.
- Draft PR presentation and lookup after remote head deletion are integrated in `53840b9db` and `9f188ccda`. Custom model pricing is integrated in `5772defe6`, including explicit zero prices and removal that restores automatic rates. Pulse usage scanning and charts are retained. Agent pricing validation passed 170 focused checks; the merged PR and pricing suites passed 31 tests. `0bb876d37` aligns four PR permission fixtures with the triage contract.
- SSH suggestions, preview tab mute, and local CLI desktop activation are integrated. Token usage normalization and additional environment metadata are still in progress. These changes are not in the locally built `.5` artifact. Full V40 compatibility remains unverified.

## Earlier checkpoint through `80672e344`

- Preview `.20260910.5` is built and locally verified from `2d86d084e8abd35199b09abe5e673fd52c52374c`, containing compatibility through `f420040f0`. It preserves published `.4` Office/Talk/downloads. Installer size is 172,946,157 bytes; SHA256 is `69e722123a53ecc8c62f32ed4beb4206243814352dcaf108d736226854dc843a`. Package source/version, Talk, and resource monitor checks pass. It is not published: automatic approval review requires explicit approval of the public repository and payload; that request is pending. The compatibility remote remains at `11c4ebc8a`.
- The first `.5` build stopped at the 80-file packaging guard. Comparing staged payloads identified exactly seven new keyring loader/native files required by browser import. `229c20840` retains a bounded 87-file budget, with 50 packaging tests passing. The rebuilt artifact has 82 files and 35 server sidecar natives.
- Android build 8 and the isolated attachment picker/add/remove cycle passed. The previously missing remove-button accessibility label is fixed in `afd9ce0ff`. Chromium attachment add/remove also passed with no page errors. These checks do not establish native media playback or installed desktop update acceptance.
- Browser profiles, explicit synthetic-tested cookie import, and capture-failure recovery are integrated through `f420040f0`. PR revert/workflow approval and Grok v5 totals are integrated. Preview server/web/desktop types and 163 merge regressions pass; 34 merged profile/quit checks and 113 merged GitHub checks pass.
- Later, unpublished work includes recording fixes for Electron 43 and hidden-preview throttling (`be2c89b67` through `708afdba4`), external project icons with the grouping-hook correction (`9fca18dcd`, `ce9e4ee12`), settle/pin/panel-close shortcuts, unpin confirmation, and persisted diff layout. Recording passed integrated desktop/web types. The frozen .6 runtime remains Electron 41.5.0; those source fixes did not themselves upgrade the dependency. See the separate [Electron 43.4.1 upgrade](t3-v040-electron43-upgrade-2026-09-10.md) for the later dependency batch and its verification limits. Preference/action tests passed 95 cases. Keyboard tests passed 86 cases; the unchanged POSIX chmod permission fixture fails on Windows. The changed server defaults pass independently.
- PR label editing and refresh subscriptions are integrated in `93fa30d00` and `80672e344`, preserving Pulse summary fallback and merge receipts. Antigravity backend/client batches remain separate work in progress. Full V40 compatibility is not claimed.

## Earlier checkpoint through `cb9f7fec2`

- Preview [pulse-preview-20260910.4](https://github.com/Polyphron-AI/pulse-code/releases/tag/pulse-preview-20260910.4) is published from `4350ae4f38771f0402545bc4a9984566a6953d95`, containing compatibility through `62d63d26a` and preserving the previous preview's Office/Talk work. The Windows x64 installer SHA256 is `1002c23568ce14e594eedb54f89ff1bcd514b375123664f3cebb4c162b5cd0bc`; the uploaded asset digest matches. Packaged server/web/desktop versions, Talk smoke checks, and resource-monitor protocol checks passed. Installed-app update acceptance remains pending.
- Later commits add the shared onboarding wizard, standalone provider installation, repository grouping, old-server import fallback, and bounded streaming of oversized transcript records. Final import validation passed 123 focused checks using synthetic histories.
- Quit confirmation now accepts legacy Boolean settings and V40 direct, hold, and double-press modes. CLI client metadata and legacy PR checkout results decode correctly. Deleted bootstrap failures carry cleanup disposition and web retries create fresh identities; the Pulse mobile pending-task retry edge remains in progress.
- Grok usage v5 is integrated in `534aef05e` with compatible v4 totals, synthetic session parsing, pricing, and web/mobile labels. Its 70 focused tests passed. Integrated mobile, web, and server typechecks passed after the usage and import batches.
- Native scrolling and coordinated thread dismissal are integrated in `0a87a185d` and `d46bbf351`, with 29 and 28 focused agent tests respectively. Android build 8 succeeded in 11m7s and its development APK was installed into the isolated emulator. Runtime acceptance is still in progress.
- The repeated Chromium attachment add/remove check passed with no page errors. These newer commits are not yet included in the published preview. Full V40 compatibility is not claimed.

## Earlier checkpoint through `5a12b7e1e`

- The compatibility branch was pushed through `cefe4e166`. Preview source `4350ae4f3` preserves published `.20260910.3` Office, Talk, MCP/skill search, dictation, and downloads, and includes compatibility through `62d63d26a`. Its `.20260910.4` installer is being rebuilt with matching server/web/desktop metadata; publication is not yet claimed.
- Native media `8ee58fbb4` and lock `87c1849c6` passed 168 agent tests and mobile types. Android build 7 succeeded in 10m10s with 835 tasks. The isolated client paired, rendered seeded messages, expanded its composer, and opened Photos/Files and the document picker. The session ended before selection/removal was verified. Playback is not yet accepted.
- Chromium attachment paste and removal passed with no page errors. The harness needed a real ClipboardEvent; the earlier synthetic event did not reach the composer correctly.
- Reset-credit contracts, scoped commands, configured hub protocol corrections, pooled limits, and local `/usage-limits` clients are integrated through `f78e5d9d1`. Redemption remains explicit and confirmed. No real credits were consumed.
- Desktop continuation `62d63d26a` prepares eligible threads at installation, clears markers on failure, and preserves them only after accepted handoff. Its 24 focused tests and server types passed. Nightly release-note counts/links through `42924220b` preserve Pulse rollback feeds; 56 backend/desktop and 46 web checks passed.
- Import foundation `8e34a5a9c` passed 328 focused agent tests. Copy-reference `fed143810` and stop shortcut `33065a21a` passed focused tests and web/mobile types; the new Android keyboard module needs a rebuild. Setup prerequisite `5a12b7e1e` passed 141 agent and 23 integrated client-runtime checks. Onboarding UI remains in progress.
- The preview merge passed server, contracts, web, desktop, and mobile types. Merged evidence includes 58 backend assets, 36 desktop/shared media, 79 web, 30 follow-up client, and 67 packaging/Office/Talk checks. These overlap agent suites and must not be summed as unique tests.

## Earlier checkpoint through `8eb5ab63d`

- Normalized subscription limits and explicitly configured quota sources now span contracts, backend, web, and mobile. Reset-credit actions and subsequent quota UI refinements remain a separate coordinated batch.
- Web sidebar arrangement now supports cross-section destinations, action labels, empty targets, pointer cancellation, and cleanup while preserving Pulse row actions. Agent validation passed 235 focused tests and web types.
- Shared media parsing, signed host media streaming, and web/desktop image, video, PDF, and HTML viewers are integrated. Mobile media, tool-result viewed images, and automatic workspace refresh remain incomplete. These source groups are recorded as partial.
- Integrated verification passed 82 focused settings, limits, pointer, and media tests, two config-stream tests, and web types. The merged server test imported Ref twice; the duplicate was removed before the backend typecheck retry.
- Isolated Chromium loads the seeded thread with an available provider and no page errors. Three settings passed save, reload, and reversal checks. The fixture now stores the canonical `codex` driver key.
- Android debug compilation succeeded with 835 tasks using a short disposable checkout and bounded build workers. The isolated emulator pairs and displays the nine seeded threads across three projects. Thread interaction and attachment acceptance are still pending; a list-rendering pass is not full mobile acceptance. Upcoming Expo Video support requires a rebuilt native client.

The earlier progress entries below retain their original evidence dates; this checkpoint supersedes their pending labels only for the behavior explicitly listed here. No installer containing this checkpoint has been published yet.

## Dependency order and evidence

| Group                                  | Source                                                                                                           | Status and integration direction                                                                                                                                                                                                              |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Correctness and initial protocol fixes | Prior [correctness](upstream-sync-2026-09-09.md) and [protocol](t3-protocol-compatibility-2026-09-09.md) ledgers | 17 selected changes landed and verified.                                                                                                                                                                                                      |
| Async questions                        | `d76b24dd1`, narrow lookup from `b17cc3d1b`, `d7fe47fd0`, `7ac93e300`, `e63ddb48e`, `7112697e8`                  | Coordinated backend/client implementation underway. Preserve OMP blocking callbacks, schedules, request finality, and pending retention.                                                                                                      |
| App/MCP approvals                      | `7c6163c67`                                                                                                      | Confirmed protocol gap: request kinds, app names, approval options, and answer mapping need adapters, contracts, and web/mobile UI together.                                                                                                  |
| Pairing metadata                       | `9d28c21a2`                                                                                                      | Integrated as `2886564a7`. Credentials only come from creation responses. Older clients with required credential fields need coordinated updates.                                                                                             |
| Used-provider restoration              | `06de9e90a`                                                                                                      | Implementation underway. Restore previously used providers after upgrades while preserving explicit disabled state and OMP.                                                                                                                   |
| Bounded reconnect replay               | `7e460f429`                                                                                                      | Integrated as `d272b7661`. SQL row and 8 MiB serialized-byte preflight runs before decoding replay. The earlier projector-backlog fix does not cover this.                                                                                    |
| Restart continuation                   | `0929907ff`, `5b7d72aad`, `b906ce2d7`, `1abc717f0`, `f47a3fe90`                                                  | Persisted opt-in, startup reconciliation, interrupted-session recovery, and shared-setting propagation. Preserve explicit provider capabilities and scheduled occurrence history.                                                             |
| Provider permissions and stop          | `43f723f80`, `994bd7373`, `d2b6f3b92`, `01f3e50ec`, `6134b90ff`                                                  | Review and port Cursor/OpenCode permissions, stop, and transport-error behavior.                                                                                                                                                              |
| Discovery and CLI fidelity             | `ea71a19d4`, `18573d60a`, `80a14b658`, `bc918e74a`, `15fea6c5f`, `2152d44de`, `a434677ec`                        | Coordinate Claude/project skills, large OpenCode skill results, and Grok CLI protocol. Preserve OMP registration.                                                                                                                             |
| Claude SDK/results                     | `560afffde`, `a5bbad910`, `940e8233c`, `4c7cd17a8`, `3d00cfd5a`, `3bf74eb6d`                                     | Upgrade SDK ^0.3.170 to target ^0.3.260 together with result/error/usage handling and bundled binary overrides.                                                                                                                               |
| Compaction/models/usage                | `c7222ca4d`, `c5ba51d62`, `5fa35d211`, `035428368`, `5a433244d`, `1587f248d`, `19d8ab2ae` and limit follow-ups   | Coordinate adapter capabilities, contracts, and clients. Preserve Pulse usage behavior; do not port reverted `535557b3f` alone.                                                                                                               |
| Project/thread persistence             | Migration groups below                                                                                           | Changes need decider/projector, snapshots, and clients, with schedule regressions.                                                                                                                                                            |
| Scoped defaults and balancing          | `9f40b2f56`, `420fd76f6`                                                                                         | Larger coordinated groups with explicit environment ownership and retained schedules/Office.                                                                                                                                                  |
| Mobile native stack                    | `3e6ab36f6`, `aab404964`, `b5a09e13f`                                                                            | Expo 57.0.18, RN 0.86.3, Reanimated 4.5.1, Worklets 0.10.1, Metro 57/screens 4.26.2 patches, exact sharing 57.0.17. Upgrade native binaries and lockfile together. Preserve Pulse fingerprints and schemes. iOS acceptance needs macOS/Xcode. |
| Remote diagnostics                     | `94f194816`                                                                                                      | Add DPoP reason fields across server/relay/clients; preserve Pulse client identities.                                                                                                                                                         |
| Media/import/setup/browser             | `8f4913221`, `beae2147a`, `775129984`, `09aac7156`, browser-profile/import series                                | Feature parity requires separate review. These are not prerequisites for decoding Codex protocol messages.                                                                                                                                    |

## Integration evidence in this batch

As of `99dcf656a`, integration includes the following additional groups. The original direction table above records the initial audit; the evidence below supersedes its in-progress labels. Full V40 compatibility is not yet established.

Later integration through `11ea38d5e` adds the following evidence:

- Generic attachments now include web file picking, bounded HEIC conversion, and mobile picker/share/draft/outbox flows. The HEIC dependency is resolved in the lockfile. Integrated web and mobile typechecks pass. The SDK 57 Android development APK compiled successfully (835 tasks) using shorter paths in a disposable checkout; emulator acceptance is underway.
- Manual compaction backend and both clients are integrated, including streamed Claude context accounting, declared adapter strategies, command availability, the web context-meter action, mobile status and terminal result rows. OMP remains explicitly unsupported for compaction. Agent checks passed 316 unique backend and 178 client tests. Integrated server typecheck passed before the later model/default batches.
- Machine/project defaults (`0c2885cb8`), bundled V40 model metadata (`7c71c12a7`), custom model descriptors (`598617718`) and opt-in environment balancing (`482b0acb9`) are integrated. Metadata stays fixed to the reviewed target rather than fetching mutable upstream-main configuration. Existing model slugs, Pulse model-list controls, OMP and user-selected draft context are preserved.
- Mobile active-thread arrangement (`f47d365c5`) passed 69 focused tests and mobile typecheck. Mixed-version environments cannot use hidden legacy auto-settled rows as arrangement anchors. Web drag-and-drop follow-ups remain in progress.
- Chromium verification exposed a configuration startup race. `7e886c0f3` acquires the settings subscription before loading the snapshot. Three focused WebSocket regressions pass. The isolated browser passes persistence and reversal checks for restart continuation, merge settlement and inactivity settlement with no page errors. Thread messages render; provider selection and attachment interaction acceptance remain under investigation.
- `11ea38d5e` corrects the composer label so an unavailable provider or required project selection is not described as a disconnected environment. Its 20 focused component checks pass.
- The first Android JavaScript load reached the app, then Metro exhausted Windows file handles while loading a dynamic filesystem module. This is not a passing mobile acceptance run. Metro is being retried with limited workers and an IPv4 endpoint reachable from the task-owned emulator. All test state is disposable and local.

- Generic approval consent was corrected in `2beed4c30`: plain Approve cannot silently select persistent approval. Restart continuation and shared settings are integrated; desktop update-specific continuation remains unadvertised until its handoff is implemented.
- Skill discovery and invocation now cover the adapted providers and web/mobile composers (`4ded10fe5`, `5bb361908`). Grok ACP support is integrated in `caa141a1d`, with 133 focused tests and one Windows-inapplicable POSIX signal case skipped. These checks use synthetic provider protocols.
- Settlement follow-ups, project auto-pull, project icons, shared PR summaries and branch associations are integrated through migrations 45–50. Pulse migration 46 intentionally preserves historical model choices because automatic and explicit selections cannot be distinguished safely. Migration 48 repairs only automatic settlement projections and preserves manual and scheduled history.
- Remote desktop updates are integrated in `7b3a10daf`, including prepare/commit coordination, reconnect verification, rollback selection, cancellation, and recovery of backend processes after failed installation. Focused updater, registry, lifecycle and client tests passed. A real installer update has not yet been exercised; newer-nightly changelog handling remains separate.
- Active thread ordering persistence is integrated in `76a39458f` with Pulse migration 51. The agent's 186 focused checks passed. Reordering controls and later drag-and-drop behavior are separate work; this foundation does not establish their completion.
- Signed attachment contracts, backend upload/download, web upload retry foundation, and shared helpers are integrated through `99dcf656a`. File uploads are bounded at 50 MiB and images retain their existing 10 MiB limit. Web generic file selection and mobile share/draft flows are still being integrated. Backend evidence includes 60 contract, 71 core/projection/capability, 54 provider/OMP and three real signed-route checks; these are agent-scoped results before final integrated client acceptance.
- The authorized isolated Chromium check loads synthetic projects and General settings without page errors. Settings persistence/reversal checks and Android emulator acceptance remain in progress. Android prebuild succeeds, but Windows native path limits and host memory pressure have delayed producing a test APK; no emulator pass is claimed.
- The published preview advanced independently to `pulse-preview-20260910.1` while this branch was being integrated. Packaging must start from the latest preview lineage to preserve its Office, Talk/dictation, MCP defaults and skill-search work. These later compatibility commits have not yet been published in an installer.

- Async questions are integrated across the provider, durable command handling, shared client runtime, web/desktop, and mobile. `d0ad3d1bf` adds terminal-first SQL lookup and retains the terminal activity for any request still visible in bounded snapshots. This prevents resolved questions from reappearing after reconnect. Backend verification included OMP callbacks and schedules; client verification included answer, dismissal, and native option mapping.
- App/MCP approvals are integrated in `4e5b75aea`, `27998f9ee`, and `4952af576`, including file-change details from `57a6b70e2`. Backend coverage passed 106 focused tests and client coverage passed 115. Independent review found that a persistence-schema default could broaden generic approval; its correction must land before release.
- Used-provider restoration is integrated in `82a32545f`, including explicit disabled-state preservation and OMP's existing default. Verification passed 133 focused tests; four existing Windows path-fixture cases were excluded after baseline comparison.
- Pairing metadata passed 23 focused store, admin, CLI, client, HTTP, and WebSocket checks. Bounded replay passed 17 focused SQL/subscription checks, including UTF-8 byte accounting and snapshot fallback. Matching clients must ship with the server because older pairing-management schemas require the removed credential field.
- Mobile SDK 57 installation and TypeScript checks pass. Android development prebuild passes with Pulse identity and schemes preserved. Expo is pinned to 57.0.18 across peer dependencies; its bundled dependency matrix accepts the patched sharing 57.0.17 and constants 57.0.16. Expo's online checker recommends newer releases, so that check is not evidence for the fixed V40 target. Native compilation and device acceptance remain pending.

## Migration allocation

Pulse has shipped `041_Issues` and `042_Integrations`. Preserve them. Allocate the upstream migrations in the same relative order using the following free IDs, only when their dependent behavior is ready.

| Upstream ID | Pulse ID | Migration                           | Source      |
| ----------- | -------- | ----------------------------------- | ----------- |
| 41          | 43       | AuthSessionClientConnection         | `11f051373` |
| 42          | 44       | ProjectionThreadLinkedPullRequest   | `3c75eb113` |
| 43          | 45       | ProjectionThreadsUnsettledAt        | `3b86ef941` |
| 44          | 46       | ClearAutomaticProjectModelDefaults  | `5392c9bb9` |
| 45          | 47       | ProjectionProjectsAutoPull          | `ba3cb0773` |
| 46          | 48       | RepairAutomaticSettlementTimestamps | `2971ec320` |
| 47          | 49       | ProjectionProjectIcon               | `f6c04c552` |
| 48          | 50       | ProjectionThreadBranchPullRequest   | `223ff4490` |
| 49          | 51       | ProjectionThreadsActiveOrderKey     | `2d645df47` |

The project-model repair rewrites historical creation events based on an upstream UI assumption. Check whether Pulse let users choose a model at project creation before applying that rewrite. Settlement repair must preserve scheduled-thread history. No migration is approved merely by allocating an ID here.

## Pulse ownership

Retain branding, CLI aliases, public endpoints, release channels, schemes, and relay identities accepting Pulse and legacy T3 clients. Preserve OMP, schedules, Office/Talk, mail dependencies, and applicable Office fixes promoted to main after preview .3.

Upstream marketing, analytics destinations, hosted infrastructure, and stable-release automation are not requirements for application compatibility. Upstream thread-feedback upload `3db38b881` is a separate data-flow decision; do not enable automatic content transmission as a side effect of protocol updates. Browser-cookie import and paid usage redemption remain explicit user actions even if their implementation is imported.

The upstream baseline remains unchanged until pending source dispositions and cross-surface verification support advancing it.
